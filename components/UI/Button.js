// nextjs button
import React from 'react';
import Link from 'next/link';

const Button = () => {
    return (
        <div>
        <button>
           button
        </button>
        </div>
    );
    }

export default Button;
